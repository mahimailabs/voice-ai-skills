[
  {"id":"review", "prompt":"Review this voice-agent design for production readiness: a cascade clinic booking agent with a fixed 500 ms silence endpoint, interruptible booking read-backs, no write deadline, only average latency logs, no telephony implementation, and no audio evals. Audit the supplied design only; do not invent source files. Name the skill you used. Return its scoring format, hard failures, and the three fixes to do first.", "expected_skill":"voice-agent-review", "criteria":["Reads SKILL.md and bundled checklist", "Uses eight groups and a 40-point total", "Identifies hard failures and three ordered fixes", "Labels missing implementation evidence rather than inventing file lines"]},
  {"id":"turn-taking", "prompt":"Our voice agent cuts callers off while they read a phone number. Its only turn rule is 500 ms of silence. Explain the likely failure and what to measure before changing the rule. Name the skill you used.", "expected_skill":"voice-turn-taking", "criteria":["Selects turn-taking and reads bundled guidance", "Distinguishes VAD from turn completion", "Tests digit pauses and uses measurements to justify changes"]},
  {"id":"latency-helper", "prompt":"Analyze this cascade voice turn: EOT 700 ms, TTFT 620 ms, TTFB 240 ms, E2E 1560 ms. Use the installed latency skill and its bundled calculator. Name the skill and show the first fix. Resolve the helper from the skill directory, not this project's working directory.", "expected_skill":"voice-latency-budget", "criteria":["Runs the packaged helper from its installed directory", "Returns a measured stage comparison", "Names endpointing as the first fix"]},
  {"id":"tool-timeout", "prompt":"Our clinic booking backend commits a reservation and then the HTTP request times out. The agent tells the caller nothing was booked and tries again. Review this behavior and propose a guarded flow. Name the skill you used.", "expected_skill":"voice-function-tools", "criteria":["Treats outcome as uncertain", "Requires read-only reconciliation and backend idempotency", "Requires completed protected read-back plus a fresh yes", "Keeps filler interruptible"]},
  {"id":"offline", "prompt":"Design the interruption rules for a clinic voice agent. Current SDK documentation is unavailable in this test. Do not browse. Apply vendor-neutral guidance and explain which implementation details you cannot verify. Name the skill you used.", "expected_skill":"voice-interruptions", "criteria":["Continues vendor-neutral analysis", "Labels SDK details unverified", "Does not invent verified method signatures"]}
]
